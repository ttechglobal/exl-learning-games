# multi-stage engine — not yet built

Planned engine for sequential rooms/questions with per-stage correctness
(architecture doc Section 1/8). No code here yet — this folder exists so the
intended structure (`MultiStageEngine.tsx`, `multiStage.config.ts`,
`multiStage.logic.ts`) is visible before it's implemented.

Build this when the next product brief calls for a mechanic genuinely
shaped like "sequential stages, each independently scored" rather than
particle-assembly's "single target, live counters, check on submit."
